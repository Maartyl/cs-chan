using System;

namespace Chan
{
  //(authority, chanName) -> {local chan, connection}
  // - ends upon all local being freed
  public class ConnectionCache {
    public ConnectionCache() {
    }
  }
}

